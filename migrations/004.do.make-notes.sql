CREATE TABLE notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    game_id INTEGER REFERENCES games(id) ON DELETE CASCADE NOT NULL,
    tab_id INTEGER REFERENCES tabs(id) ON DELETE CASCADE NOT NULL,
    title TEXT NOT NULL,
    contents TEXT
);